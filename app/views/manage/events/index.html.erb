<h1>Listing manage_events</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @manage_events.each do |manage_event| %>
  <tr>
    <td><%= link_to 'Show', manage_event %></td>
    <td><%= link_to 'Edit', edit_manage_event_path(manage_event) %></td>
    <td><%= link_to 'Destroy', manage_event, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_manage_event_path %>
