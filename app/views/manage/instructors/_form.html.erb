<%= form_for(@manage_instructor) do |f| %>
  <% if @manage_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@manage_instructor.errors.count, "error") %> prohibited this manage_instructor from being saved:</h2>

      <ul>
      <% @manage_instructor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
